# In development, Vite's dev server proxies /api requests to the backend
# (see vite.config.js). No env vars are strictly required for local dev.
#
# For production builds where the frontend and backend are on different
# domains, set this and update src/services/api.js to use it as baseURL:
VITE_API_URL=http://localhost:5000/api
